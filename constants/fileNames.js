const ATTENDANCE_FILE_NAME = 'attendance-data'
const ATTENDANCE_SEED_FILE_NAME = 'attendance-seed-data'

const STUDENTS_SEED_FILE_NAME = 'students-seed-data'

module.exports = {
  ATTENDANCE_FILE_NAME,
  ATTENDANCE_SEED_FILE_NAME,
  STUDENTS_SEED_FILE_NAME,
}
